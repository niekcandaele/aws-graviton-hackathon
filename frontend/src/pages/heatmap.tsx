import { useEffect, useRef } from 'react';

import HeatmapHandler from '../components/heatmapHandler';

export default function Heatmap() {

  return (
    <div>
      <h1>Heatmaps</h1>
      {/* <HeatmapHandler canvas="canvas"></HeatmapHandler> */}
    </div>
  );
}
